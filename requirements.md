# GoosePerception - Requirements

See `notes/` for working documents:
- `notes/plan.md` - Implementation status and next steps
- `notes/original-spec.md` - Original requirements (snapshot)
- `notes/agent-actions-implementation.md` - Future: CLI agent integration

## Quick Commands

```bash
just build    # Build .app bundle
just run      # Build and launch
just clean    # Clean artifacts
```

## Known Issues

- Screen capture may need permission re-grant after rebuilding
- Run from terminal to see debug output: `./GoosePerception/build/GoosePerception.app/Contents/MacOS/GoosePerception`
